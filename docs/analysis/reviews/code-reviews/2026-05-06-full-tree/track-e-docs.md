# Track E — Documentation & ADR consistency

- **Agent run by:** Claude general-purpose agent (Opus 4.7), 2026-05-06
- **Scope:** Doc accuracy, ADR consistency, audit-log↔code 1:1 correspondence, umbrix→tyrne residue.
- **HEAD reviewed:** `214052d`

## Findings (flat list, classified)

### Blocker

- [`docs/architecture/overview.md:77`](../../../../architecture/overview.md) — BSP table claims `bsp-qemu-virt` uses **GICv3**, but T-012 shipped a GIC v2 driver (`bsp-qemu-virt/src/gic.rs`, ADR-0011 + `exceptions.md` + UNSAFE-2026-0019 all say "GIC v2"). GICv3 contradicts every other authoritative document for the same target and would mislead any reader who navigates from `overview.md` → BSP code.
- [`docs/architecture/hal.md:50`](../../../../architecture/hal.md) — BSP layering Mermaid box reads `BIrq["GICv3 / GIC-400 impl"]`. `bsp-qemu-virt` ships GICv2; `bsp-pi4`'s GIC-400 is also a GICv2 (compatible subset) — there is no GICv3 impl in tree.
- [`docs/architecture/hal.md:181`](../../../../architecture/hal.md) — `bsp-qemu-virt` table row "Interrupt controller | GICv3" (same drift as overview.md). The `bsp-pi4` row at line 198 correctly notes GIC-400 (v2 subset).
- [`docs/architecture/hal.md:126`](../../../../architecture/hal.md) — Timer subsection still asserts *"The IRQ-armed half of the trait — `arm_deadline` / `cancel_deadline` — is `unimplemented!()` in QEMU virt's BSP today: it depends on the GIC + EL1 exception-vector-table install that T-012 (B1) will land."* T-012 closed 2026-04-28 (commit `b4ed68c`); both methods have real bodies in `bsp-qemu-virt/src/cpu.rs` lines 492 / 511 (audited under UNSAFE-2026-0021); ADR-0010 §Revision notes already records this. The hal.md prose is stale.
- [`docs/architecture/scheduler.md:11`](../../../../architecture/scheduler.md) and [line 73](../../../../architecture/scheduler.md) — Both passages still describe idle's body as `core::hint::spin_loop()` and frame the `wait_for_interrupt` form as a *future* T-012 deliverable ("ADR-0022's first rider clarifies that the idle task's body uses `core::hint::spin_loop` until T-012…", and "The idle task's body in v1 is `core::hint::spin_loop()` followed by `yield_now`, not `wait_for_interrupt`, because no IRQ source is configured before T-012. When T-012 lands, the body switches to…"). T-012 landed 2026-04-28; `bsp-qemu-virt/src/main.rs:276` shows `cpu.wait_for_interrupt();` in `idle_entry`, exactly as `exceptions.md` §"Idle's `wfi` activation" describes. scheduler.md is the only architecture doc still in the pre-T-012 idle shape.
- [`docs/architecture/security-model.md:330`](../../../../architecture/security-model.md) — *Open questions* still says *"v1's `boot.s` on QEMU `virt` does not explicitly `msr daifset, #0xf` before stack / BSS setup — the reset state happens to leave interrupts effectively masked … but this is a per-platform accident rather than a guarantee. Future BSPs should adopt 'mask DAIF first' as a standard reset-vector prologue; add the instruction to the BSP boot checklist…"* T-013 (commit `f289d4d`, ADR-0024, audited under UNSAFE-2026-0017) added exactly this `msr daifset, #0xf` as the **first** instruction of `_start` (`bsp-qemu-virt/src/boot.s:84`). The Open question is closed; either remove the bullet or convert it to a cross-reference rider noting "closed by T-013 / UNSAFE-2026-0017".
- [`docs/glossary.md:25`](../../../../glossary.md) — Live Markdown link `(decisions/0023-cross-table-capability-revocation-policy.md)` points at a file that does not exist; ADR-0023 is reserved-empty (no file at HEAD). The plan §Track E checklist explicitly asks "verify no live link points to a missing 0023". The glossary is the only remaining live link to a missing 0023 (every other reference uses the literal text `ADR-0023` without a hyperlink, e.g. `phase-b.md`, `security-model.md`, B0/B1 closure reviews). Resolution: either drop the hyperlink or change the parenthetical to "see ADR-0023 (deferred per Phase B0 closure)" without the link.

### Non-blocking

- [`README.md:39-55`](../../../../README.md) — *Repository layout* tree omits the four crates that exist in the workspace at HEAD (`kernel/`, `hal/`, `bsp-qemu-virt/`, `test-hal/`, `tools/`) and explicitly says *"Source code layout — the Rust workspace, HAL crates, userspace services — will be added after the architecture phase."* Phase A and B0/B1 are complete; ~9 590 LOC of Rust + asm exist. The status banner at the top is up-to-date; only the tree + the trailing sentence are stale.
- [`CONTRIBUTING.md:13-14`](../../../../CONTRIBUTING.md) — *"Pull requests against source code. There is no source code to extend or refactor meaningfully yet."* Stale: kernel/HAL/BSP/test-HAL crates exist with 149 host tests + B1 closure done.
- [`SECURITY.md:7`](../../../../SECURITY.md) — *"Tyrne is **pre-alpha**. There is no runnable kernel yet."* The kernel boots on QEMU virt and runs the two-task IPC demo end-to-end (see `docs/guides/two-task-demo.md`); the second sentence is now factually wrong even though "pre-alpha" remains accurate.
- [`SECURITY.md:7`](../../../../SECURITY.md) — *"The formal threat model is a work in progress and will be documented in `docs/architecture/security-model.md` (planned, Phase 2)."* security-model.md exists at HEAD and is `Accepted` per `docs/architecture/README.md`. The "(planned, Phase 2)" qualification is stale.
- [`docs/standards/README.md`](../../../../standards/README.md) — Standards index lists 13 documents; the directory contains 14 standards (the README is missing `bsp-boot-checklist.md`). The file is referenced from `unsafe-policy.md §5a`, `security-model.md §Open questions`, and elsewhere — it is a real, in-use standard, just absent from the index.
- [`docs/architecture/README.md:13`](../../../../architecture/README.md) — The `overview.md` index row says simply *"Accepted"* with no v0.0.1 status qualifier, but the doc itself bears a *"Status (2026-04-27)"* banner that mentions Phase A only and does not yet enumerate B0 / B1 closure (the banner predates the GIC-version drift this report's first finding flags). Worth aligning when the GIC-version finding is resolved — if `overview.md` gets a status sweep, append B0/B1 closure to the banner alongside the GIC fix.
- [`docs/guides/two-task-demo.md:47`](../../../../guides/two-task-demo.md) — *"a dedicated `wfe`-based idle path is Phase B work (see ADR-0019 open questions)"* — Phase B1 already lit up `wait_for_interrupt` in `idle_entry`. The trailing remark belongs in the *Known limitations (Phase A)* section's archive but is no longer the reality at HEAD. The actual `task A` path still ends in `core::hint::spin_loop()` (main.rs:471), so the *spin-loop continues to print no further output* claim is correct; it is the "Phase B work" framing that is stale.
- [`docs/guides/two-task-demo.md:49-59`](../../../../guides/two-task-demo.md) — Execution trace says *"the scheduler adds Task B first, then Task A, so B runs first"*. Truthful as far as it goes — `bsp-qemu-virt/src/main.rs:695-702` adds B, then A, then idle — but the guide does not mention the **idle task** at all. Idle is the third task added and is structurally on the FIFO between A and B's later yields (per ADR-0022 + scheduler.md). Adding one sentence of acknowledgement ("a third task `idle_entry` is registered after A; it never reaches the head while A and B are progressing the demo") would close the gap without rewriting the trace.
- [`docs/guides/two-task-demo.md:38-45`](../../../../guides/two-task-demo.md) — Expected-output table omits two production lines emitted by the current main.rs: `tyrne: timer ready ({} Hz, resolution {} ns)` (line 611) and `tyrne: boot-to-end elapsed = {n} ns` (line 468). These were added by T-009. The trace as written *will* match if you look at it for the IPC-relevant lines only, but a strict line-by-line comparison fails.
- [`docs/architecture/overview.md:67-73`](../../../../architecture/overview.md) — *"Expected HAL trait surface (final form documented in `hal.md`, planned)"* — hal.md is `Accepted` and shipped; the parenthetical "planned" is stale. (Same nit applies to *"Final IPC primitive set"* in *Open questions* line 233 — settled by ADR-0017, accept-Done — but that line is more legitimately framed as future-work-on-fastpath, so I leave it.)
- [`docs/decisions/template.md`](../../../../decisions/template.md) — Template still lists the *Status* enum as `Proposed | Accepted | Deprecated | Superseded by NNNN`. ADR-0025 introduces `Deferred` as a real state (ADR-0018 uses it for reply-recv; ADR-0023 / ADR-0026 referenced as deferred elsewhere). Either add `Deferred` to the template's enum or document that "Deferred" lives in the body rather than the Status field. Minor; the README already mentions ADR-0018 as `Deferred` even though the file's Status reads `Accepted`.

### Observation

- [`docs/audits/unsafe-log.md`](../../../../audits/unsafe-log.md) — All 21 entries (UNSAFE-2026-0001..0021; 0012 `Removed`) follow `unsafe-policy.md §3` discipline; the introducing-commit-vs-merge boundary is now codified in 0017's "Discipline note" paragraph and observed by 0006 / 0011 / 0014 / 0015 / 0016 / 0017 / 0018 Amendment blocks. Indexing is contiguous (no holes besides the 0012 archived-removed entry).
- [`docs/decisions/0024-el-drop-policy.md`](../../../../decisions/0024-el-drop-policy.md) and [`0025-adr-governance-amendments.md`](../../../../decisions/0025-adr-governance-amendments.md) — Both ADRs from-0024-onward correctly include the *Dependency chain* section per ADR-0025 §Rule 1; template.md exposes the same section. No regression.
- ADR frontmatter spot-checked across 0001..0025: every ADR in the tree has a populated `Status` / `Date` / `Deciders` triple. No malformed frontmatter.
- ADR cross-references inspected: ADR-0010 cites T-009 + T-012; ADR-0021 cites T-006 / UNSAFE-2026-0012 / 0013 / 0014 (and the 2026-04-28 IRQ-handler Amendment); ADR-0022 cites T-007 / T-009 / T-012 (closing the first-rider Sub-rider); ADR-0024 cites T-013 + UNSAFE-2026-0017 / 0016 / 0018; ADR-0025 cites the rider-discipline precedents. Every Accepted ADR's referenced T-NNN exists and is `Done`.
- No `Supersedes` / `Superseded by` pairs exist in the tree (no superseded ADRs at HEAD); symmetry obligation is vacuous.
- Audit-log ↔ source-code SAFETY-block correspondence: every active UNSAFE-2026-NNNN tag has at least one `// SAFETY:` block in source citing it (table below). No stranded audit entries; no untagged production `unsafe` blocks. The few `// SAFETY:` comments without an explicit `Audit:` tag are all inside `#[cfg(test)]` test modules of `kernel/src/sched/mod.rs` (lines 857..1525), which is consistent with `unsafe-policy.md` (the production blocks all carry tags; test code is held to the SAFETY-comment rule but audit-log entries are about production unsafety).
- `git grep -i 'umbrix'` on the tree (excluding `docs/analysis/technical-analysis/`) finds **zero** hits in source / standards / ADRs / architecture / guides / glossary / root docs. Two hits remain inside review artefacts (the full-tree review plan + the B1 closure retro), both legitimately discussing the rename's history. The 10e3351 sweep did close the residue.
- [`docs/guides/run-under-qemu.md`](../../../../guides/run-under-qemu.md) — Verified that `tools/run-qemu.sh` exists and matches the cargo runner referenced in `.cargo/config.toml`; the `0x40080000` entry-point claim matches `linker.ld`'s `MEMORY` block; `qemu-system-aarch64 -M virt -cpu cortex-a72 …` invocation matches the actual runner used by the workspace.
- [`docs/guides/ci.md`](../../../../guides/ci.md) — `.github/workflows/ci.yml` exists at HEAD (created 2026-04-27); the four-job table matches the workflow content (`lint-and-host-test`, `kernel-build`, `miri`, `coverage`); pinned versions (`NIGHTLY_PIN=nightly-2026-01-15`, `cargo-llvm-cov 0.6.16`) are documented. ci.md is consistent with the workflow file as it stands.
- [`docs/architecture/exceptions.md`](../../../../architecture/exceptions.md) — Fully consistent with T-012 source; design-first authoring + the implementation-map table calling out which commit landed which step makes drift mechanically harder.
- [`docs/architecture/boot.md`](../../../../architecture/boot.md) — Consistent with the post-T-013 `_start` (lines 79-129 reproduce the actual `boot.s`). The `halt_unsupported_el` named-label form matches both source and the UNSAFE-2026-0017 §Operation correction Amendment.
- [`docs/architecture/ipc.md`](../../../../architecture/ipc.md) — Consistent with kernel/src/ipc/mod.rs and ADR-0017 / 0018 / 0021 cross-references; resume-path `IpcError::PendingAfterResume` matches the source.
- [`docs/standards/unsafe-policy.md`](../../../../standards/unsafe-policy.md) — All 21 audit entries observe §1 (SAFETY comment with invariants / rejected alternatives / audit reference), §2 (rustdoc `# Safety` on `unsafe fn`), §3 (append-only with Amendment / Removed-status disciplines), §4 (`unsafe impl` discipline), §5a (naked-asm rule for context-switch — `bsp-qemu-virt/src/cpu.rs` `context_switch_asm` honours the rule), §7 (small `unsafe` block scope under `#![deny(unsafe_op_in_unsafe_fn)]`).
- [`docs/roadmap/current.md`](../../../../roadmap/current.md) — Status is up-to-date through B1 closure; "Next task to open" + "Active task: none" are coherent.

## Audit-log ↔ source-code SAFETY-block correspondence

Source files scanned: `kernel/src/`, `hal/src/`, `bsp-qemu-virt/src/`. Every "Code SAFETY block present" reference is the file path the SAFETY comment lives in; the audit-log location field already names the same file.

| Audit tag | Status | Code SAFETY block present | Consistent? |
|---|---|---|---|
| UNSAFE-2026-0001 | Active | `bsp-qemu-virt/src/main.rs` `kernel_entry` (multiple `Audit: UNSAFE-2026-0001` citations at lines 508, 515, 521, 523, 576, 618, 628, 670, 706, 731) | yes |
| UNSAFE-2026-0002 | Active | `bsp-qemu-virt/src/main.rs:731` (panic handler — *"`Audit: UNSAFE-2026-0002`"*) | yes |
| UNSAFE-2026-0003 | Active | `bsp-qemu-virt/src/console.rs:50` (`unsafe impl Send` — *"Audit: UNSAFE-2026-0003"*) | yes |
| UNSAFE-2026-0004 | Active | `bsp-qemu-virt/src/console.rs:57` (`unsafe impl Sync` — *"Audit: UNSAFE-2026-0004"*) | yes |
| UNSAFE-2026-0005 | Active | `bsp-qemu-virt/src/console.rs:70` (`Pl011Uart::write_bytes` — *"Audit: UNSAFE-2026-0005"*) | yes |
| UNSAFE-2026-0006 | Active (+ 2026-04-23 Amendment) | `bsp-qemu-virt/src/cpu.rs:212` and `:221` (*"Audit: UNSAFE-2026-0006"* and *"Audit: UNSAFE-2026-0006 (post-T-009 scope under the 2026-04-23 Amendment)"*) | yes |
| UNSAFE-2026-0007 | Active | `bsp-qemu-virt/src/cpu.rs:229, 247, 264, 273, 281` (DAIF / WFI / ISB sites — *"Audit: UNSAFE-2026-0007"*) | yes |
| UNSAFE-2026-0008 | Active | `bsp-qemu-virt/src/cpu.rs:346, 408` (`context_switch_asm` + caller); `kernel/src/sched/mod.rs:484, 490, 591, 795` (split-borrow + start) | yes |
| UNSAFE-2026-0009 | Active | `bsp-qemu-virt/src/cpu.rs:428` (`init_context`); `kernel/src/sched/mod.rs:256` (`add_task` forwarding) | yes |
| UNSAFE-2026-0010 | Active | `bsp-qemu-virt/src/main.rs:81, 258, 299, 392, 462, 333, 443` (`StaticCell::Sync` + per-static citations) | yes |
| UNSAFE-2026-0011 | Active (+ 2026-04-23 Amendment) | `bsp-qemu-virt/src/main.rs:138, 159` (`TaskStack::Sync` + `TaskStack::top`) | yes |
| UNSAFE-2026-0012 | **Removed** (commit `f9b72f8`) | none expected — pattern was retired; `kernel/src/sched/mod.rs:369, 397, 430, 542, 639, 810` cite it as the *hazard the new pattern avoids* (correct historical reference) | yes |
| UNSAFE-2026-0013 | Active | `bsp-qemu-virt/src/main.rs:108` (`StaticCell::as_mut_ptr`); citations at 278, 316, 715 | yes |
| UNSAFE-2026-0014 | Active (+ 2026-04-27 + 2026-04-28 Amendments) | `kernel/src/sched/mod.rs:432, 479, 543, 640, 671, 728, 760, 812`; `bsp-qemu-virt/src/main.rs:281, 316, 352, 406, 426, 634, 718` | yes |
| UNSAFE-2026-0015 | Active (+ 2026-04-23 Amendment) | `bsp-qemu-virt/src/cpu.rs:115, 172, 472` (`new` + `now_ns` reads) | yes |
| UNSAFE-2026-0016 | Active (+ 2026-04-27 / T-013 Amendment) | `bsp-qemu-virt/src/cpu.rs:111, 132, 148` (the assertion call site; citations of the Amendment in module doc) | yes |
| UNSAFE-2026-0017 | Active (+ 2026-04-27 PR-#9 round-1 + round-2 Amendments) | `bsp-qemu-virt/src/boot.s:39` ("Audit: UNSAFE-2026-0017"); referenced from `cpu.rs:154, 457, 494` and `exceptions.rs:190` | yes |
| UNSAFE-2026-0018 | Active (+ 2026-04-27 cfg-gating Amendment) | `hal/src/cpu.rs:163, 178` (the `current_el()` helper); cited from `bsp-qemu-virt/src/cpu.rs:110, 132, 148, 453` | yes |
| UNSAFE-2026-0019 | Active (Pending QEMU smoke verification) | `bsp-qemu-virt/src/gic.rs` 24 distinct citation sites (lines 19, 86, 115, 152, 157, 164, 178, 202, 225, 235, 242, 257, 262, 270, 275, 285, 289, 297, 301, 310, 338, 360, 368, 382); `cpu.rs:525, 549`; `main.rs:568, 576, 581` | yes |
| UNSAFE-2026-0020 | Active (Pending QEMU smoke verification) | `bsp-qemu-virt/src/vectors.s:5`; `exceptions.rs:17, 143, 164, 255`; `main.rs:481, 553, 595` | yes |
| UNSAFE-2026-0021 | Active (Pending QEMU smoke verification) | `bsp-qemu-virt/src/cpu.rs:499, 512, 537`; `exceptions.rs:203` | yes |

Indexing is contiguous (0001..0021 with 0012 archived); no audit entry is stranded; no production `unsafe` block lacks an audit-tag citation.

## Cross-track notes

- → Track A (kernel correctness): every kernel-side `// SAFETY:` block in `kernel/src/sched/mod.rs` is correctly tagged in the audit table above; no scope discrepancy detected in the doc-vs-code direction. (Track A owns the inverse direction — *does the SAFETY reasoning still hold under current code?*)
- → Track B (HAL & test-HAL): `hal/src/cpu.rs` `current_el()` helper is the only `unsafe` site in the HAL crate per the audit log; `hal.md`'s Timer subsection at line 126 ("`unimplemented!()` in QEMU virt's BSP today") is the documentation-side drift Blocker above. Track B should confirm the implementation side matches ADR-0010's *Decision outcome* contract independently.
- → Track G (BSP & boot path): `boot.md` and `exceptions.md` faithfully reproduce the post-T-013 `_start` and the post-T-012 vector-table install. Any G-side finding about the ordering of `MSR VBAR_EL1` vs `MSR DAIFClr` should cross-check against `boot.md` Phase 3 + `exceptions.md` §"IRQ-handler interaction" — both docs agree the unmask happens after vector + GIC init.
- → Track J (umbrix→tyrne residue): zero residue confirmed in scope (excluding `docs/analysis/technical-analysis/` per the plan + the legitimate review-plan / B1-retro mentions). The 10e3351 sweep closed the rename completely on the documentation surface this track owns.
- → Merge step: the seven Blockers are all single-edit fixes in single files; none requires a code change. Resolution is mechanical: update the four GIC-version mentions, swap "v1's `boot.s` does not …" for a closure note, swap idle's body description from spin_loop to wait_for_interrupt in scheduler.md (twice), swap `unimplemented!()` for "implemented per ADR-0010 §Revision notes 2026-04-28" in hal.md Timer subsection, drop or de-link the missing-0023 path in glossary.md.

## Sub-verdict

**Request changes**

Rationale: seven Blocker-class drift items (six architecture/security-model docs describing the system as it was at B0 closure rather than at HEAD post-B1, plus one dead link to ADR-0023). None are security-relevant — every Blocker is a *factual statement that has fallen out of sync with the code* — but they all violate the Track E acceptance bar of "every doc still describes the code as it is". The non-blocking items are real but lower-stakes (root-doc tree obsolescence, standards index missing one entry, guide trace omissions). The audit-log ↔ source correspondence table came up clean, ADR frontmatter and Dependency-chain disciplines are in order, and umbrix→tyrne residue is fully closed.

---

Track E summary lines:

- Seven Blockers identified: four GIC-version drift items (overview.md / hal.md ×2 / hal Mermaid), one stale "scheduler.md idle = spin_loop" pair, one stale "hal.md Timer = unimplemented!()" line, one already-closed open-question in security-model.md (DAIF mask), and one live-link to a missing ADR-0023 in glossary.md.
- Non-blocking drift in root docs (README/CONTRIBUTING/SECURITY) plus standards-index missing `bsp-boot-checklist.md`; guide gaps for idle task + post-T-009 boot prints.
- Audit log is 1:1 with source SAFETY blocks (21 entries, all tagged in code; 0012 correctly Removed); umbrix→tyrne residue verified clean per the 10e3351 sweep.
