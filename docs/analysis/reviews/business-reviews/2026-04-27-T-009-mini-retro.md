# Business review 2026-04-27 — T-009 mini-retro

- **Trigger:** maintainer-initiated mini-retro at the close of T-009's three review-fix rounds and the ADR-0013 lessons-into-rules codification.
- **Scope:** T-009 (Timer init + `CNTVCT_EL0` measurement) — commits `0b0d993..328cab5` — plus the meta-arc this task surfaced: the codification of ADR cool-down + forward-reference contract + rider-normal rules in ADR-0013 (`56fd9eb`), and the opening of T-012 to ground a previously-handwaved forward-reference (`ca962d6`).
- **Period:** 2026-04-23 (T-009 opened) → 2026-04-27 (today; ADR-0013 update + T-012 Draft + T-009 third-round review-fixes landed).
- **Participants:** @cemililik (+ Claude Opus 4.7 agent as scribe; two independent review agents during the second-read pass).

---

## What landed

### Commits (eleven, in three logical clusters)

**Cluster 1 — T-009 implementation (4 commits, 2026-04-23):**

| SHA | Subject |
|-----|---------|
| `0b0d993` | `docs(roadmap): open T-009 — timer init + CNTPCT_EL0 measurement` |
| `beb0963` | `feat(bsp): implement Timer for QemuVirtCpu via CNTPCT_EL0 / CNTFRQ_EL0` |
| `55f2d10` | `feat(bsp): instrument kernel_entry boot-to-end measurement` |
| `1df3641` | `docs(roadmap): T-009 → In Review; doc sweep` |

**Cluster 2 — Three-round review fix-ups (5 commits, 2026-04-23 / -27):**

| SHA | Subject |
|-----|---------|
| `39fb66c` | `fix(hal,bsp,docs): T-009 second-read review fixes — register family + arithmetic + audit policy` |
| `80e2be3` | `docs(audits): back-fill UNSAFE-2026-0006 / 0015 Amendment commit SHAs` |
| `db3a4c7` | `fix(bsp,docs): close R1 Yüksek #1 — runtime EL check missing from 39fb66c` |
| `01010f4` | `fix(ci,hal,docs): R3-round review fixes — install guard, explicit zero-freq panic, audit-log readability` |
| `328cab5` | `fix(hal,bsp,docs): R3-round review fixes — stale comments, T-009 rename, resolution floor` |

**Cluster 3 — Lessons-into-rules + forward-references grounded (2 commits, 2026-04-27):**

| SHA | Subject |
|-----|---------|
| `56fd9eb` | `docs(roadmap,adr): codify ADR cool-down + dependency-chain + rider hygiene (ADR-0013 amendment + template + skill)` |
| `ca962d6` | `docs(roadmap): open T-012 — exception and IRQ infrastructure (B1, Draft)` |

### ADRs / audit entries

- **ADR-0010** (Timer trait, accepted 2026-04-20) — BSP-side first real implementation lands.
- **ADR-0022 first rider** — gains a *Sub-rider* clarifying the T-009 (time source) vs T-012 (IRQ delivery) two-task path. Sub-rider grounded at T-012's task file in commit `ca962d6`.
- **ADR-0013** (roadmap & planning, originally accepted 2026-04-20) — amended in-place to codify three new rules: ADR cool-down (no same-day Accept), Forward-reference contract (every "future task" claim grounded at a real T-NNN), Riders-are-not-failures (frequency is the signal, not presence).
- **Template + write-adr skill** — `Dependency chain` subsection added to MADR template; skill procedure gains a cool-down step and a dependency-chain ACı.
- **UNSAFE-2026-0015** (CNTVCT/CNTFRQ MRS reads) — created in `beb0963`, gained two Amendments (CNTPCT→CNTVCT register-family swap; readability "casual reader" pointer).
- **UNSAFE-2026-0006** (QemuVirtCpu Send/Sync) — gained an Amendment for the post-T-009 struct shape (the original "zero-size type with no fields" body became inaccurate when T-009 added two `u64` fields).
- **UNSAFE-2026-0016** (boot-time `CurrentEL` self-check) — created in `db3a4c7` after the maintainer caught that R1.1's runtime-check half had been silently skipped.

### Tests / verification

- 77 kernel + **17 hal** (was 0 → 13 → 15 → 17) + 34 test-hal = **128 host tests green**.
- `cargo +nightly miri test --workspace --exclude tyrne-bsp-qemu-virt` — clean across all 128 tests.
- QEMU smoke — A6 five-line trace + T-009's two-line additions, byte-for-byte stable across the eleven commits.

### Tasks closed / opened

- **T-009** moved through `Draft → In Progress → In Review` (still awaiting Done promotion).
- **T-012** opened as `Draft` (exception + IRQ infrastructure, B1) — first task to ground a previously-handwaved forward-reference under the new ADR-0025 rule.

## What changed in the plan

- **B1 milestone retitled** from "Drop to EL1 in boot" to "Drop to EL1 in boot, **install exception infrastructure**". B1 sub-breakdown gains item 5 referencing T-012. B1 acceptance criteria gains the closure condition for ADR-0022's first-rider Sub-rider ("idle's body uses `wait_for_interrupt`").
- **B0 sub-breakdown item 5** (Timer init) clarified: original wording said "CNTPCT_EL0"; T-009 second-read surfaced the register-family mismatch and the implementation switched to `CNTVCT_EL0`. Phase plan retains a parenthetical pointing at the change.
- **ADR-0013 gains three new rules** under §"Integration with ADRs". This is the meta-change: not just one task's plan, but the rules under which every future plan/decision is made.
- **No new ADR** during T-009's arc — but **ADR-0024 is now scheduled as next**, per the user's chosen C → A → B sequence. ADR-0024's draft is the first real test of the cool-down + dependency-chain rules.

## What we learned

**Rules work better than re-discovered lessons.** Across A → B0, the same dependency-analysis weakness surfaced four times (ADR-0021 mid-proposal revision, ADR-0021 post-Accept rider, ADR-0022 first rider, ADR-0022 first-rider sub-rider). T-006 mini-retro had named the lesson — "trace the call graph" — but the lesson stayed at the prose level, didn't ship as a rule. T-009's arc reproduced the failure once more (ADR-0022 first rider conflated time-source with IRQ delivery), and only then did we sit down and codify the rule. **The practical implication:** lessons-as-prose tend to be re-discovered; lessons-as-rules in ADR-0013 / template / skills tend to fire on the next attempt. We have not yet validated the codified rules — ADR-0024 is the validation event. Until then, the rules are paper, not progress.

**Reviews left their own stale documentation behind.** The five findings in the third-round review (commit `328cab5`) were almost entirely *generated by* the second-round fix commits (`39fb66c`, `db3a4c7`, `01010f4`). Each fix commit corrected one type of staleness while leaving another type behind: the second-round renamed `cntpct → cntvct` in code but not in task-file title; the runtime-EL-check commit added the assertion but not the doc-comment audit-tag note; the explicit-panic commit added asserts but missed the resolution-floor edge case. The pattern is **mini-rider chains within the rider response itself** — one level of recursion below the ADR/rider chain we already knew about. The third-round was the maintainer's own catch ("are all accepted findings actually fixed?"), not an agent-driven find. **The implication:** even with a strong review process, expect the *fixes* to need a small follow-up sweep one round after they land. Build that into DoD rather than be surprised by it.

**User-driven thoroughness beats agent-driven thoroughness when the agent has been at it for a while.** The maintainer's question "kabul edilen bulguların tamamı düzeltildi mi?" (are all accepted findings actually fixed?) caught the silently-skipped runtime EL check — a real correctness gap. The agent had self-reported "all accepted findings fixed"; the maintainer's verification surfaced that this was wrong. This is the same lesson as the T-006 retro's "post-In-Review second-read" gate, applied one level deeper: the *fix-up commits* themselves need a verification gate. The user is providing this manually today; whether to automate it (e.g. an "all-findings-actually-applied" agent pass) is open.

**File renames have a long tail.** Renaming `T-009-timer-init-cntpct.md → -cntvct.md` required updates in 4 other files (phase-b/README.md, phase-b.md, current.md, T-012). Without `grep -rln` discipline this is easy to miss. We should normalize: any file rename in `docs/analysis/tasks/` or `docs/decisions/` triggers a `grep -rln <old-slug>` step before commit. Cheap; would have caught two of the third-round review's findings preemptively.

## Adjustments

- [ ] **Validate the new ADR-0025 rules with ADR-0024.** Per the user's chosen C→A→B sequence, ADR-0024 (EL drop policy) is next. Land it as `Proposed` today; Accept tomorrow per cool-down. Whether this prevents the rider-thrash pattern is the open empirical question. If ADR-0024 needs riders within a week, the rules need refining — escalate. If not, the rules are real progress.
- [ ] **Add a stale-link CI check.** A small script that grep's `docs/` for filename references that don't resolve to actual files. Catches the rename-tail problem mechanically rather than relying on review eyes. Low effort; high catch rate. Route to whoever picks up the next CI work (could fold into T-006's R6 follow-ups).
- [ ] **Open T-008 (architecture docs, B0)** before B0 closes. Currently the only B0 task without a file. Per the new forward-reference contract, every milestone exit requires every milestone task to exist as a file (Draft is fine). Trigger: do this in the C→A→B sequence's B step.
- [ ] **Codify "post-fix sweep" in DoD.** Every review-fix commit's DoD should include "grep for stale references to the entity I just renamed/changed". Add to commit-style.md or task-template DoD. Trigger: amend `commit-style.md` after ADR-0024 lands (so we batch process discipline changes).

## Next

- **Active phase:** B
- **Active milestone:** B0 (T-006/T-007/T-009 `In Review`; T-008 to open; T-011 `Draft`) + B1 (T-012 `Draft`).
- **Active task:** None — T-009 in review, awaiting maintainer Done promotion.
- **Next review trigger:** ADR-0024 cool-down period (Proposed → Accepted ≥ 24 hours) is the first observable test of the new rules; the next *retro* trigger is B0 closure when T-006/T-007/T-009/T-011 all reach `Done`. If ADR-0024 ships clean — no rider needed in its first week — that becomes a positive data point worth noting in the B0 retro.
